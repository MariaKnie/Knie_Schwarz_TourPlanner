<UserControl x:Class="Knie_Schwarz_TourPlanner_project.Views.TourList"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="50" d:DesignWidth="300">
    <Grid>
        <Grid x:Name="LeftSide">
            <Viewbox Margin="5">
                <ListView x:Name="RouteList1" ItemsSource="{Binding routeManagementVM.RouteList, AsyncState=true}" SelectedItem="{Binding routeManagementVM.ActiveRoute}">
                    <ListView.View>
                        <GridView>
                            <GridViewColumn Header="Tours" Width="200" DisplayMemberBinding="{Binding RouteName}"/>
                        </GridView>
                    </ListView.View>
                </ListView>
            </Viewbox>
        </Grid>
    </Grid>
    
</UserControl>
