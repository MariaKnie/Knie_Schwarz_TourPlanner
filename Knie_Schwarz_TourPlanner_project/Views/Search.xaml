<UserControl x:Class="Knie_Schwarz_TourPlanner_project.Views.Search"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="50" d:DesignWidth="300">

    <Grid x:Name="Searchbar" >
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="15" />
                <ColumnDefinition Width="Auto" MinWidth="55" />
            </Grid.ColumnDefinitions>

            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <TextBox Text="Search..."/>


            <GridSplitter Grid.Column="1" Width="5" HorizontalAlignment="Center" VerticalAlignment="Stretch" />
        <StackPanel Grid.Column="2" Orientation="Horizontal" >
                <Button Grid.Column="2" Content="Search" />
                <Button Grid.Column="2" Content="Clear" />
            </StackPanel>
            </Grid>
        <!-- Your search bar UI components here --><!--
            <TextBox x:Name="txtSearch" Text="Search..." VerticalAlignment="Center"/>
            <Button Content="Search" Click="SearchButton_Click" VerticalAlignment="Center"/>-->

    </UserControl>

